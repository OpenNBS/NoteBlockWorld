{
  "name": "note-block-world",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "pnpm run --parallel dev",
    "dev:web": "pnpm run --filter ./web dev",
    "dev:server": "pnpm run --filter ./server dev",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prettier": "prettier --write ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^2.8.8"
  },
  "dependencies": {
    "@encode42/nbs.js": "^4.0.1"
  }
}
