{
  "extends": "../../tsconfig.package.json",
  "compilerOptions": {
    "outDir": "dist",

    // DOM support for canvas operations
    "lib": ["dom", "dom.iterable", "esnext"]
  },
  "include": ["src/**/*.ts"]
}
