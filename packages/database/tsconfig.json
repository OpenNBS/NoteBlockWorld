{
  "extends": "../../tsconfig.package.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist",

    // Database runtime requirements
    "emitDecoratorMetadata": true
  },
  "include": ["src/**/*.ts"]
}
