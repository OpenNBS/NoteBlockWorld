// TODO: write tests

// test that there are no gaps in each column of the song

// test that all tempo changers are kept

// test that there's only one tempo changer instrument and it's the last one

// test that no instruments are unused

// test that all instruments are named an empty string or 'Tempo Changer'

// test that all layers have all properties set to the default values (name empty, unlocked, volume 100, panning 0)

// test that no silent notes are present

// test that song stats are cleared
