'use client';
import { GetServerSideProps } from 'next';

type queryParam = {
  token?: string;
  page?: string;
};

const ExternalLoginPage = () => {
  return <></>;
};

export default ExternalLoginPage;
